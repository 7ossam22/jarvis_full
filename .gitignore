# Your real API key lives here — never commit it. Copy config.example.json to
# get started.
config.json

# The self-signed TLS certificate + private key generated on first https
# start. Machine-local (it names this host's LAN addresses), never source.
certs/

# Live-grown notes captured via voice ("remember that…") and the generated
# graph data are local state, not source.
notes/captures/
viewer/graph-data.js

# Python
__pycache__/
*.py[cod]
*.jarvis-bak
.venv/
.venv-browser/
.venv-kokoro/
venv/
tools/browser_daemon.log

# OS
.DS_Store
Thumbs.db
*.log
